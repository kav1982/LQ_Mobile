# 项目协作规范

本项目是 Unity + C# + HotFix 工程。以下规范适用于整个工程。

## 项目事实

- 本工程没有 `.cursor/rules/*.mdc`，不要搜索、读取或要求补充这些文件。
- 本工程没有 `openspec/`，不要将检查或补充 openspec 作为开始任务的前置条件。
- 判断实际行为时，以当前代码、资源配置和 Unity 运行结果为准；文档仅作为辅助依据。

## 沟通与开始条件

- 始终使用中文回答，先给结论，再说明原因和改动建议。
- 开始任务前，先说明对目标、交付物和假设的理解；如有必要，每次最多提出 3 个关键问题。
- 在用户明确回复“可以开始”前，不写代码、不修改文件。
- 引用文件时优先使用项目相对路径，例如 `Assets/GameScripts/Example.cs:42`。

## 开发约束

- 修改或分析文件前先阅读相关上下文，实施最小必要改动，避免无关重构。
- 保持现有命名、分层、region、注释和异步风格与项目一致。
- 项目异步代码统一使用 UniTask 风格，不随意引入 `Task` 风格实现。
- 发现问题时优先说明根因并正确修复，不通过随意增加容错代码掩盖问题。
- 涉及新功能或较大改动时，先检查工程内实际存在的相关设计文档和现有实现；没有设计文档不构成阻塞。
- 工作区可能包含用户未提交的改动，必须保留并避开无关变更。

## Unity 验证

- Shader 和渲染问题应交叉检查 Shader、材质、渲染管线、相机和 Renderer Feature 等实际配置。
- 修改后按风险执行 Unity 重新导入、编译、控制台检查、测试或视觉截图验证。
- 验证结果需区分本次新增问题与修改前已存在的问题。
